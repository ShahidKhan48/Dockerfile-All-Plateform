# Use official Nginx image from DockerHub
FROM nginx:latest

# Copy the static website content to the Nginx default location
COPY ./html /usr/share/nginx/html

# Expose port 80 to serve the web traffic
EXPOSE 80

# Start Nginx
CMD ["nginx", "-g", "daemon off;"]



####Port: 80 (standard for HTTP)
###Context: This Dockerfile assumes you have your HTML files in a folder named html at the root of your project.
