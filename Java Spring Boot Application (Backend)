# Use an official OpenJDK image
FROM openjdk:17

# Set the working directory inside the container
WORKDIR /app

# Copy the Spring Boot jar to the container
COPY target/myapp.jar /app/myapp.jar

# Expose port 8080 for Spring Boot
EXPOSE 8080

# Run the Spring Boot application
CMD ["java", "-jar", "myapp.jar"]


#Port: 8080 (default port for Spring Boot applications)
#Context: You need to package your Spring Boot application into a jar file (myapp.jar) and place it in the target folder.
